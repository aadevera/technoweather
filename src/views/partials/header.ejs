<nav class="px-3 navbar navbar-expand-md sticky-top">
  <a class="navbar-brand" href="#">
    <img
      src="https://static.cdnlogo.com/logos/w/44/weather-ios.svg"
      alt="Logo"
      width="30"
      height="24"
      class="d-inline-block align-text-top"
    />
    TechnoWeather
  </a>
  <button
    class="navbar-toggler navbar-toggler-right"
    type="button"
    data-bs-toggle="collapse"
    data-bs-target="#navb"
    aria-expanded="true"
  >
    <span class="navbar-toggler-icon"></span>
  </button>
  <div id="navb" class="navbar-collapse collapse hide">
    <ul class="navbar-nav">
      <li class="nav-item active">
        <a class="nav-link" href="#">Home</a>
      </li>
    </ul>

    <ul class="nav navbar-nav ms-auto">
      <% if (user) { %>
      <li class="nav-item navbar-brand">Welcome <%= user.username %>!</li>
      <li class="nav-item">
        <form id="logoutForm">
          <button type="submit" class="btn btn-danger" id="logoutButton">
            <span>Logout</span>
          </button>
        </form>
      </li>
      <% } else { %>
      <li class="nav-item">
        <a
          class="nav-link"
          href="#"
          data-bs-toggle="modal"
          data-bs-target="#signUpModal"
          ><span class="fas fa-user"></span> Sign Up</a
        >
      </li>
      <li class="nav-item">
        <a
          class="nav-link"
          href="#"
          data-bs-toggle="modal"
          data-bs-target="#loginModal"
        >
          <span class="fas fa-sign-in-alt"></span> Login</a
        >
      </li>
      <% } %>
    </ul>
  </div>
</nav>
